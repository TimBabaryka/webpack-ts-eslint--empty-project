export interface Count {
  num: string,
  name: string,
  count: string,
  year: string,
  shape: string,
  color: string,
  size: string,
  favorite: boolean;
}

export interface Particles {
  x: number,
  y:number,
  opacity: number,
  speedX: number,
  speedY: number,
  radius: number,
}
